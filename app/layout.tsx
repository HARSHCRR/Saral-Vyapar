import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Nivesh Mitra - Single Window System, Government of Uttar Pradesh',
  description: 'Single Window System for business approvals and investor assistance in Uttar Pradesh',
  keywords: 'Nivesh Mitra, UP Government, Single Window System, Business Approvals, Investor Assistance',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
} 